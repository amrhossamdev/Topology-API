package models


//primary constructor for device specs
open class DeviceSpecs(
    Default: Double?,
    Max: Double?,
    Min: Double?
) {
    var default = Default
    var max = Max
    var min = Min


}